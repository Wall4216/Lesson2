<Page x:Class="Lesson2.Pages.AddDataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Add Data Page">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Height" Value="28"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <StackPanel Width="300">
            <TextBlock Text="Добавьте новое блюдо" FontWeight="Bold" FontSize="18" Margin="0,0,0,20"/>
            <TextBlock Text="Название:"/>
            <TextBox x:Name="NameTextBox"/>
            <TextBlock Text="Цена:"/>
            <TextBox x:Name="PriceTextBox"/>
            <TextBlock Text="Стоимость:"/>
            <TextBox x:Name="CostTextBox"/>
            <Button Content="Добавить" Click="AddDataButton_Click"/>
            <Button Content="Посмотреть блюда" Click="ViewDataButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
