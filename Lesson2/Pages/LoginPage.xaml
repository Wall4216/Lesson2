<Page x:Class="Lesson2.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="LoginPage">
    <Grid Background="#EEE">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Добро пожаловаать" FontSize="24" Margin="0,0,0,20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBox x:Name="UsernameTextBox" Height="40" Margin="10" />
            <PasswordBox x:Name="PasswordBox" Height="40" Margin="10" />
            <Button Content="Login" Height="40" Margin="10" Click="LoginButton_Click" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>
